<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.realk.thekootwit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/title_activity_main" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="628058551296aa799e3f227046d366f3a0df71da" />

        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.SearchActivity"
            android:label="@string/title_activity_search"></activity>
        <activity
            android:name=".activity.ListManagerActivity"
            android:label="@string/title_activity_list_manager"></activity>
        <activity
            android:name=".activity.TimelineActivity"
            android:label="@string/title_activity_timeline"></activity>
    </application>

</manifest>
